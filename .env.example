# Database connection (PostgreSQL)
# Automatically provided on Railway when adding a PostgreSQL plugin
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Web Server Port (Defaults to 3000)
PORT=3000
NODE_ENV=production
